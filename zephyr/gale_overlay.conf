# Phase 1+2: Proven modules with decision struct pattern (all CI-tested)
CONFIG_GALE_KERNEL_SEM=y
CONFIG_GALE_KERNEL_MUTEX=y
CONFIG_GALE_KERNEL_CONDVAR=y
CONFIG_GALE_KERNEL_MSGQ=y
CONFIG_GALE_KERNEL_STACK=y
CONFIG_GALE_KERNEL_PIPE=y
CONFIG_GALE_KERNEL_EVENT=y
CONFIG_GALE_KERNEL_TIMER=y
CONFIG_GALE_KERNEL_MEM_SLAB=y
CONFIG_GALE_KERNEL_FIFO=y
CONFIG_GALE_KERNEL_LIFO=y
CONFIG_GALE_KERNEL_QUEUE=y
CONFIG_GALE_KERNEL_MBOX=y
CONFIG_GALE_KERNEL_TIMEOUT=y

# Phase 3: Disabled until C shims are verified against this Zephyr version.
# Decision structs and FFI functions exist in ffi/src/lib.rs but the C shims
# reference struct members that may differ across Zephyr versions.
# Enable individually after testing:
# CONFIG_GALE_KERNEL_POLL=y       # needs CONFIG_POLL + version-specific struct members
# CONFIG_GALE_KERNEL_FUTEX=y      # needs CONFIG_USERSPACE
# CONFIG_GALE_KERNEL_TIMESLICE=y  # needs version-specific timeslicing API
# CONFIG_GALE_KERNEL_KHEAP=y      # needs sys_heap internal API
# CONFIG_GALE_KERNEL_THREAD_LIFECYCLE=y  # needs sched internals
# CONFIG_GALE_KERNEL_WORK=y       # needs work queue internals
# CONFIG_GALE_KERNEL_FATAL=y      # conflicts with always-compiled fatal.c
# CONFIG_GALE_KERNEL_MEMPOOL=y    # needs heap_pool internals
# CONFIG_GALE_KERNEL_DYNAMIC=y    # needs CONFIG_DYNAMIC_THREAD
# CONFIG_GALE_KERNEL_SMP_STATE=y  # needs CONFIG_SMP
# CONFIG_GALE_KERNEL_SCHED=y      # needs sched internals
