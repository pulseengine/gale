# Requirements — ASPICE V-model traceability from stakeholder through SW level.
#
# Hierarchy:
#   STKH-001 (stakeholder)
#     → SYSREQ-SEM-001 (system) → SWREQ-SEM-P01..P10 (software)
#     → SYSREQ-MUT-001 (system) → SWREQ-MUT-M01..M11 (software)
#     → SYSREQ-CV-001  (system) → SWREQ-CV-C01..C08  (software)
#     → SYSREQ-MQ-001  (system) → SWREQ-MQ-MQ01..MQ13 (software)
#     → SYSREQ-SK-001  (system) → SWREQ-SK-SK01..SK09 (software)
#     → SYSREQ-PP-001  (system) → SWREQ-PP-PP01..PP10 (software)
#     → SYSREQ-TM-001  (system) → SWREQ-TM-TM01..TM08 (software)
#     → SYSREQ-EV-001  (system) → SWREQ-EV-EV01..EV08 (software)
#     → SYSREQ-MS-001  (system) → SWREQ-MS-MS01..MS08 (software)
#
# The verified properties are the ASIL-D formal specifications.
# Each maps directly to a property in the Verus/Rocq specs.

artifacts:
  # ── Stakeholder requirement ──────────────────────────────────────────────
  - id: STKH-001
    type: stakeholder-req
    title: Formally verified kernel primitives for ASIL-D
    status: approved
    description: >
      Replace Zephyr kernel synchronization primitives (sem.c, mutex.c, etc.)
      with formally verified Rust implementations that preserve identical
      functional behavior while proving safety properties to ASIL-D level
      via SMT solving (Verus) and theorem proving (Rocq).
    tags: [safety, asil-d, formal-verification]
    fields:
      priority: must
      source: "ISO 26262 ASIL-D — highest automotive safety integrity level"
    links:
      - type: derives-from
        target: ZEP-SYRS-14

  # ── System requirement ───────────────────────────────────────────────────
  - id: SYSREQ-SEM-001
    type: system-req
    title: Counting semaphore with init/give/take/reset
    status: approved
    description: >
      A counting semaphore that matches the Zephyr k_sem API semantics:
      initialization with count and limit, give (signal), take (wait),
      and reset.  Must maintain priority-ordered wait queue for blocked
      threads and never exhibit undefined behavior or arithmetic overflow.
    tags: [sem, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 10 properties (P1-P10) verified by Verus SMT proofs,
        Rocq theorem proofs, and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001
      - type: derives-from
        target: ZEP-SYRS-14

  # ── Software requirements: the 10 ASIL-D properties ─────────────────────
  #
  # Each traces to SYSREQ-SEM-001 AND the specific Zephyr upstream SRS
  # requirements it satisfies (from zephyrproject-rtos/reqmgmt semaphore.sdoc).

  - id: SWREQ-SEM-P01
    type: sw-req
    title: "P1: 0 <= count <= limit (always)"
    status: approved
    description: >
      The semaphore count is always between 0 and limit inclusive.
      This is the fundamental invariant — it must hold after init, give,
      take, reset, and any interleaving of operations.
    tags: [sem, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures/invariant clause. Proptest random op sequences.
        Integration tests for init, give, take boundaries.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-2
      - type: satisfies
        target: ZEP-SRS-5-4
      - type: satisfies
        target: ZEP-SRS-5-5
      - type: satisfies
        target: ZEP-SRS-5-15

  - id: SWREQ-SEM-P02
    type: sw-req
    title: "P2: limit > 0 (always)"
    status: approved
    description: >
      The semaphore limit is always strictly positive.
      Enforced by init rejecting limit == 0 with -EINVAL.
    tags: [sem, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus requires clause on init. Unit test for zero limit rejection.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-18

  - id: SWREQ-SEM-P03
    type: sw-req
    title: "P3: give with no waiters increments count, capped at limit"
    status: approved
    description: >
      When give is called and the wait queue is empty:
      if count < limit, count increments by 1 (GiveResult::Incremented).
      If count == limit, count is unchanged (GiveResult::Saturated).
      Maps to z_impl_k_sem_give lines 109-111 in kernel/sem.c.
    tags: [sem, give, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on give(). Unit tests for increment and saturation.
        Proptest gives_saturate_at_limit.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-12
      - type: satisfies
        target: ZEP-SRS-5-13

  - id: SWREQ-SEM-P04
    type: sw-req
    title: "P4: give with waiters wakes highest-priority thread, count unchanged"
    status: approved
    description: >
      When give is called and the wait queue is non-empty, the
      highest-priority (numerically lowest) waiting thread is woken with
      return value 0 (OK).  The count does not change.
      Maps to z_impl_k_sem_give lines 103-108 in kernel/sem.c.
    tags: [sem, give, wake, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on give(). Integration test give_wakes_highest_priority_thread.
        Wait queue ordering test.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-14

  - id: SWREQ-SEM-P05
    type: sw-req
    title: "P5: take when count > 0 decrements by exactly 1"
    status: approved
    description: >
      When take is called and count > 0, count decrements by 1 and
      returns 0 (OK).
      Maps to z_impl_k_sem_take lines 143-148 in kernel/sem.c.
    tags: [sem, take, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on try_take(). Unit test take_decrements_by_one.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-6
      - type: satisfies
        target: ZEP-SRS-5-7

  - id: SWREQ-SEM-P06
    type: sw-req
    title: "P6: take when count == 0, no wait, returns -EBUSY"
    status: approved
    description: >
      When take is called with K_NO_WAIT and count == 0, returns -EBUSY
      without modifying the semaphore.
      Maps to z_impl_k_sem_take lines 150-154 in kernel/sem.c.
    tags: [sem, take, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on try_take(). Unit test take_empty_returns_ebusy.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-11

  - id: SWREQ-SEM-P07
    type: sw-req
    title: "P7: take when count == 0, with wait, blocks calling thread"
    status: approved
    description: >
      When take is called with a timeout and count == 0, the calling
      thread is blocked and inserted into the wait queue in priority order.
      Maps to z_impl_k_sem_take line 158 (z_pend_curr) in kernel/sem.c.
    tags: [sem, take, blocking, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on take_blocking(). Unit test take_blocking_enqueues_thread.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-8

  - id: SWREQ-SEM-P08
    type: sw-req
    title: "P8: reset sets count to 0, wakes all waiters with -EAGAIN"
    status: approved
    description: >
      Reset clears count to 0 and wakes every waiting thread with
      return value -EAGAIN.  Returns the number of threads woken.
      Maps to z_impl_k_sem_reset lines 166-192 in kernel/sem.c.
    tags: [sem, reset, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on reset(). Integration test reset_clears_everything.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-16
      - type: satisfies
        target: ZEP-SRS-5-17

  - id: SWREQ-SEM-P09
    type: sw-req
    title: "P9: no arithmetic overflow in any operation"
    status: approved
    description: >
      No arithmetic operation on count or limit causes integer overflow
      or underflow.  Give at limit saturates; take at 0 returns error.
      The C code uses (count != limit) ? 1U : 0U pattern (line 110).
      Gale uses checked_add/saturating_sub.
    tags: [sem, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus overflow-free proof. Miri UB detection. Clippy
        arithmetic_side_effects lint (deny). Integration test
        no_overflow_at_u32_max_limit.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001

  - id: SWREQ-SEM-P10
    type: sw-req
    title: "P10: wait queue ordering preserved across all operations"
    status: approved
    description: >
      The wait queue maintains priority ordering (numerically lowest
      priority value = highest priority) at all times.  Threads are
      woken strictly in priority order.
    tags: [sem, wait-queue, ordering, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus sorted-invariant on WaitQueue. Integration test
        wait_queue_ordering_across_operations. Unit test
        pend_maintains_priority_order.
    links:
      - type: derives-from
        target: SYSREQ-SEM-001
      - type: satisfies
        target: ZEP-SRS-5-14

  # ── Mutex: System requirement ─────────────────────────────────────────────

  - id: SYSREQ-MUT-001
    type: system-req
    title: Reentrant mutex with ownership tracking
    status: approved
    description: >
      A reentrant mutex that matches the Zephyr k_mutex API semantics:
      initialization, lock (reentrant), unlock with ownership checks,
      and priority-ordered wait queue for blocked threads.  Must maintain
      the owner/lock_count correspondence invariant and never exhibit
      undefined behavior or arithmetic overflow.
    tags: [mutex, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 11 properties (M1-M11) verified by Verus SMT proofs,
        Rocq theorem proofs, and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── Mutex: Software requirements (11 ASIL-D properties) ──────────────────

  - id: SWREQ-MUT-M01
    type: sw-req
    title: "M1: lock_count > 0 iff owner.is_some() (always)"
    status: approved
    description: >
      The fundamental mutex invariant: the lock count is positive if and only
      if the mutex has an owner.  This must hold after init, lock, unlock,
      and any interleaving of operations.
    tags: [mutex, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus biconditional in inv(). Integration tests m1_invariant_*.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M02
    type: sw-req
    title: "M2: wait_q non-empty implies mutex is locked (always)"
    status: approved
    description: >
      Waiters can only exist when the mutex is locked.  An unlocked mutex
      always has an empty wait queue.
    tags: [mutex, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus implication in inv(). Integration test m2_waiters_implies_locked.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M03
    type: sw-req
    title: "M3: try_lock when unlocked sets owner, lock_count = 1"
    status: approved
    description: >
      When try_lock is called on an unlocked mutex, the caller becomes the
      owner and lock_count is set to 1.
      Maps to z_impl_k_mutex_lock lines 121-131 in kernel/mutex.c.
    tags: [mutex, lock, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on try_lock(). Integration test m3_lock_unlocked_mutex.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M04
    type: sw-req
    title: "M4: try_lock by same owner increments lock_count (reentrant)"
    status: approved
    description: >
      When try_lock is called by the current owner, lock_count is incremented.
      The owner remains the same.
      Maps to z_impl_k_mutex_lock line 129 in kernel/mutex.c.
    tags: [mutex, lock, reentrant, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on try_lock(). Integration test m4_reentrant_lock.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M05
    type: sw-req
    title: "M5: try_lock by different thread returns WouldBlock"
    status: approved
    description: >
      When try_lock is called by a thread other than the owner, it returns
      WouldBlock without modifying the mutex state.
      Maps to z_impl_k_mutex_lock line 148 in kernel/mutex.c.
    tags: [mutex, lock, contention, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on try_lock(). Integration test m5_lock_contention.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M06
    type: sw-req
    title: "M6: unlock by non-owner returns error (EINVAL/EPERM)"
    status: approved
    description: >
      Unlocking a mutex that is not locked returns -EINVAL.  Unlocking a
      mutex owned by a different thread returns -EPERM.  In both cases,
      the mutex state is unchanged.
      Maps to z_impl_k_mutex_unlock lines 238-250 in kernel/mutex.c.
    tags: [mutex, unlock, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on unlock(). Integration tests m6a/m6b_unlock_not_*.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M07
    type: sw-req
    title: "M7: unlock when lock_count > 1 decrements count, owner unchanged"
    status: approved
    description: >
      When unlock is called by the owner and lock_count > 1, the count
      is decremented and the owner remains the same (reentrant release).
      Maps to z_impl_k_mutex_unlock lines 266-269 in kernel/mutex.c.
    tags: [mutex, unlock, reentrant, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on unlock(). Integration test m7_reentrant_unlock.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M08
    type: sw-req
    title: "M8: unlock with waiters transfers ownership to highest-priority"
    status: approved
    description: >
      When unlock is called by the owner and lock_count == 1 with waiters
      present, ownership is transferred to the highest-priority waiting
      thread.  lock_count stays at 1, the waiter is woken with return
      value 0 (OK).
      Maps to z_impl_k_mutex_unlock lines 278-296 in kernel/mutex.c.
    tags: [mutex, unlock, transfer, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on unlock(). Integration test m8_ownership_transfer.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M09
    type: sw-req
    title: "M9: unlock with no waiters fully unlocks (count=0, owner=None)"
    status: approved
    description: >
      When unlock is called by the owner and lock_count == 1 with no
      waiters, the mutex is fully unlocked: owner cleared to None and
      lock_count set to 0.
      Maps to z_impl_k_mutex_unlock lines 297-299 in kernel/mutex.c.
    tags: [mutex, unlock, full-release, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on unlock(). Integration test m9_full_unlock.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M10
    type: sw-req
    title: "M10: no arithmetic overflow in lock_count"
    status: approved
    description: >
      No arithmetic operation on lock_count causes integer overflow.
      The Verus code requires lock_count < u32::MAX as precondition.
      The plain code uses checked_add.
    tags: [mutex, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus overflow-free proof (requires clause). Integration test
        m10_no_overflow. Clippy arithmetic_side_effects lint.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  - id: SWREQ-MUT-M11
    type: sw-req
    title: "M11: wait queue ordering preserved across all operations"
    status: approved
    description: >
      The wait queue maintains priority ordering at all times.  Threads
      waiting on the mutex are unblocked strictly in priority order
      (numerically lowest value first).
    tags: [mutex, wait-queue, ordering, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus sorted-invariant via WaitQueue::inv(). Integration test
        m11_priority_ordering.
    links:
      - type: derives-from
        target: SYSREQ-MUT-001

  # ── CondVar: System requirement ──────────────────────────────────────────

  - id: SYSREQ-CV-001
    type: system-req
    title: Condition variable with signal/broadcast/wait
    status: approved
    description: >
      A condition variable that matches the Zephyr k_condvar API semantics:
      initialization, signal (wake one), broadcast (wake all), and wait
      (block on condition).  Must maintain priority-ordered wait queue
      and never exhibit undefined behavior or arithmetic overflow.
    tags: [condvar, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 8 properties (C1-C8) verified by Verus SMT proofs
        and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── CondVar: Software requirements (8 ASIL-D properties) ────────────────

  - id: SWREQ-CV-C01
    type: sw-req
    title: "C1: after init, wait queue is empty"
    status: approved
    description: >
      After initialization, the condition variable's wait queue has
      zero entries and the invariant holds.
      Maps to condvar.c:21-30.
    tags: [condvar, init, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on init(). Unit test condvar_init_empty.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  - id: SWREQ-CV-C02
    type: sw-req
    title: "C2: signal wakes at most one waiter (highest priority)"
    status: approved
    description: >
      When signal is called and the wait queue is non-empty, exactly
      one thread (the highest-priority waiter) is woken and the wait
      queue length decreases by 1.
      Maps to condvar.c:44-61.
    tags: [condvar, signal, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on signal(). Integration test signal_wakes_one.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  - id: SWREQ-CV-C03
    type: sw-req
    title: "C3: signal on empty condvar is a no-op"
    status: approved
    description: >
      When signal is called and the wait queue is empty, the condvar
      state is unchanged.
      Maps to condvar.c:44-61.
    tags: [condvar, signal, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on signal(). Unit test signal_empty_noop.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  - id: SWREQ-CV-C04
    type: sw-req
    title: "C4: broadcast wakes all waiters, returns woken count"
    status: approved
    description: >
      Broadcast clears the wait queue entirely and returns the number
      of threads woken, equal to the previous queue length.
      Maps to condvar.c:73-96.
    tags: [condvar, broadcast, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on broadcast(). Integration test broadcast_wakes_all.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  - id: SWREQ-CV-C05
    type: sw-req
    title: "C5: broadcast on empty condvar returns 0"
    status: approved
    description: >
      When broadcast is called on an empty wait queue, zero threads
      are woken and the condvar state is unchanged.
      Maps to condvar.c:73-96.
    tags: [condvar, broadcast, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on broadcast(). Unit test broadcast_empty_returns_zero.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  - id: SWREQ-CV-C06
    type: sw-req
    title: "C6: wait adds thread to wait queue (blocking path)"
    status: approved
    description: >
      When wait_blocking is called, the thread is blocked and inserted
      into the wait queue in priority order.  Queue length increases by 1.
      Maps to condvar.c:99-121.
    tags: [condvar, wait, blocking, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on wait_blocking(). Unit test wait_enqueues_thread.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  - id: SWREQ-CV-C07
    type: sw-req
    title: "C7: signal/broadcast preserve wait queue ordering"
    status: approved
    description: >
      After signal or broadcast, the condvar invariant (wait queue
      sorted by priority) is maintained.
      Maps to condvar.c:44-96.
    tags: [condvar, wait-queue, ordering, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus inv() maintained across signal/broadcast.
        Integration test ordering_preserved.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  - id: SWREQ-CV-C08
    type: sw-req
    title: "C8: no arithmetic overflow in broadcast woken count"
    status: approved
    description: >
      The broadcast woken count cannot overflow because it is bounded
      by MAX_WAITERS (64), which fits in u32.
    tags: [condvar, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus bounded proof (woken <= 64). Clippy arithmetic_side_effects lint.
    links:
      - type: derives-from
        target: SYSREQ-CV-001

  # ── MsgQ: System requirement ─────────────────────────────────────────────

  - id: SYSREQ-MQ-001
    type: system-req
    title: Message queue with ring buffer semantics
    status: approved
    description: >
      A message queue that matches the Zephyr k_msgq API semantics:
      initialization, put (enqueue), put_front (priority enqueue),
      get (dequeue), peek, and purge.  Must maintain ring buffer index
      consistency and never exhibit undefined behavior or arithmetic overflow.
    tags: [msgq, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 13 properties (MQ1-MQ13) verified by Verus SMT proofs
        and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── MsgQ: Software requirements (13 ASIL-D properties) ──────────────────

  - id: SWREQ-MQ-MQ01
    type: sw-req
    title: "MQ1: 0 <= used_msgs <= max_msgs (capacity invariant)"
    status: approved
    description: >
      The message count is always between 0 and max_msgs inclusive.
      This is the fundamental invariant maintained across all operations.
      Maps to msg_q.c:43-470.
    tags: [msgq, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures/invariant on all methods. Proptest random op sequences.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ02
    type: sw-req
    title: "MQ2: read_idx < max_msgs (index bounds)"
    status: approved
    description: >
      The read index is always strictly less than max_msgs, ensuring
      valid ring buffer access.
      Maps to msg_q.c:43-470.
    tags: [msgq, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus invariant on all methods.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ03
    type: sw-req
    title: "MQ3: write_idx < max_msgs (index bounds)"
    status: approved
    description: >
      The write index is always strictly less than max_msgs, ensuring
      valid ring buffer access.
      Maps to msg_q.c:43-470.
    tags: [msgq, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus invariant on all methods.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ04
    type: sw-req
    title: "MQ4: msg_size > 0, max_msgs > 0 (always)"
    status: approved
    description: >
      Both message size and maximum count are always strictly positive.
      Enforced by init rejecting zero values with -EINVAL.
      Maps to msg_q.c:43-71.
    tags: [msgq, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus requires on init(). Unit test init_rejects_zero.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ05
    type: sw-req
    title: "MQ5: put on non-full queue increments used_msgs, advances write_idx"
    status: approved
    description: >
      When put is called and the queue is not full, used_msgs increments
      by 1 and write_idx advances to the next ring buffer slot.
      Maps to msg_q.c:130-228.
    tags: [msgq, put, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on put(). Unit test put_increments_count.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ06
    type: sw-req
    title: "MQ6: put on full queue returns ENOMSG, state unchanged"
    status: approved
    description: >
      When put is called and the queue is full, returns -ENOMSG
      without modifying the queue state.
      Maps to msg_q.c:130-228.
    tags: [msgq, put, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on put(). Unit test put_full_returns_enomsg.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ07
    type: sw-req
    title: "MQ7: put_front on non-full queue retreats read_idx correctly"
    status: approved
    description: >
      When put_front is called and the queue is not full, used_msgs
      increments by 1 and read_idx retreats to the previous ring buffer slot.
      Maps to msg_q.c:236-239.
    tags: [msgq, put-front, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on put_front(). Unit test put_front_retreats_read_idx.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ08
    type: sw-req
    title: "MQ8: get on non-empty queue decrements used_msgs, advances read_idx"
    status: approved
    description: >
      When get is called and the queue is not empty, used_msgs decrements
      by 1 and read_idx advances to the next ring buffer slot.
      Maps to msg_q.c:280-349.
    tags: [msgq, get, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on get(). Unit test get_decrements_count.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ09
    type: sw-req
    title: "MQ9: get on empty queue returns ENOMSG, state unchanged"
    status: approved
    description: >
      When get is called and the queue is empty, returns -ENOMSG
      without modifying the queue state.
      Maps to msg_q.c:280-349.
    tags: [msgq, get, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on get(). Unit test get_empty_returns_enomsg.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ10
    type: sw-req
    title: "MQ10: peek_at computes correct slot index"
    status: approved
    description: >
      peek_at returns the correct ring buffer slot index for a given
      logical position within the occupied entries.
      Maps to msg_q.c:397-430.
    tags: [msgq, peek, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on peek_at(). Unit test peek_at_correct_index.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ11
    type: sw-req
    title: "MQ11: purge resets to empty (used_msgs=0, read_idx=write_idx)"
    status: approved
    description: >
      Purge clears all messages, setting used_msgs to 0 and aligning
      read_idx with write_idx.
      Maps to msg_q.c:443-470.
    tags: [msgq, purge, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on purge(). Unit test purge_empties_queue.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ12
    type: sw-req
    title: "MQ12: no arithmetic overflow in any operation"
    status: approved
    description: >
      No arithmetic operation on indices or counts causes integer
      overflow.  Uses u64 intermediate for index calculations that
      may exceed u32 range.
    tags: [msgq, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus overflow-free proof. Clippy arithmetic_side_effects lint.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  - id: SWREQ-MQ-MQ13
    type: sw-req
    title: "MQ13: ring buffer consistency (write_idx tracks read_idx + used_msgs)"
    status: approved
    description: >
      The ring buffer maintains the consistency invariant:
      write_idx == (read_idx + used_msgs) % max_msgs.
      This is verified across all operations.
      Maps to msg_q.c:43-470.
    tags: [msgq, invariant, ring-buffer, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ring_consistent() invariant on all methods.
    links:
      - type: derives-from
        target: SYSREQ-MQ-001

  # ── Stack: System requirement ────────────────────────────────────────────

  - id: SYSREQ-SK-001
    type: system-req
    title: LIFO stack with push/pop semantics
    status: approved
    description: >
      A LIFO stack that matches the Zephyr k_stack API semantics:
      initialization with capacity, push (increment), and pop (decrement).
      Must maintain count/capacity bounds and never exhibit undefined
      behavior or arithmetic overflow.
    tags: [stack, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 9 properties (SK1-SK9) verified by Verus SMT proofs
        and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── Stack: Software requirements (9 ASIL-D properties) ──────────────────

  - id: SWREQ-SK-SK01
    type: sw-req
    title: "SK1: 0 <= count <= capacity (bounds invariant)"
    status: approved
    description: >
      The stack count is always between 0 and capacity inclusive.
      This is the fundamental invariant maintained across all operations.
      Maps to stack.c:27-190.
    tags: [stack, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures/invariant on all methods. Proptest random op sequences.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK02
    type: sw-req
    title: "SK2: capacity > 0 (always after init)"
    status: approved
    description: >
      The stack capacity is always strictly positive.
      Enforced by init rejecting capacity == 0 with -EINVAL.
      Maps to stack.c:27-42.
    tags: [stack, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus requires on init(). Unit test init_rejects_zero.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK03
    type: sw-req
    title: "SK3: push when not full increments count by 1"
    status: approved
    description: >
      When push is called and count < capacity, count increments by 1
      and returns OK.
      Maps to stack.c:101-136.
    tags: [stack, push, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on push(). Unit test push_increments_count.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK04
    type: sw-req
    title: "SK4: push when full returns ENOMEM, state unchanged"
    status: approved
    description: >
      When push is called and count >= capacity, returns ENOMEM
      without modifying the stack state.
      Maps to stack.c:101-136.
    tags: [stack, push, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on push(). Unit test push_full_returns_enomem.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK05
    type: sw-req
    title: "SK5: pop when not empty decrements count by 1"
    status: approved
    description: >
      When pop is called and count > 0, count decrements by 1
      and returns OK.
      Maps to stack.c:148-190.
    tags: [stack, pop, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on pop(). Unit test pop_decrements_count.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK06
    type: sw-req
    title: "SK6: pop when empty returns EBUSY, state unchanged"
    status: approved
    description: >
      When pop is called and count == 0, returns EBUSY
      without modifying the stack state.
      Maps to stack.c:148-190.
    tags: [stack, pop, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on pop(). Unit test pop_empty_returns_ebusy.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK07
    type: sw-req
    title: "SK7: num_free + num_used == capacity (conservation)"
    status: approved
    description: >
      The sum of free and used slots always equals the capacity.
      Maps to stack.c:27-190.
    tags: [stack, conservation, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures on num_free()/num_used(). Proptest conservation check.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK08
    type: sw-req
    title: "SK8: no arithmetic overflow in any operation"
    status: approved
    description: >
      No arithmetic operation on count or capacity causes integer
      overflow.  All arithmetic is guarded by bounds checks.
    tags: [stack, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus overflow-free proof. Clippy arithmetic_side_effects lint.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  - id: SWREQ-SK-SK09
    type: sw-req
    title: "SK9: push-pop roundtrip preserves state"
    status: approved
    description: >
      A push followed by a pop returns count to its original value.
    tags: [stack, roundtrip, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on push() then pop(). Proptest roundtrip check.
    links:
      - type: derives-from
        target: SYSREQ-SK-001

  # ── Pipe: System requirement ─────────────────────────────────────────────

  - id: SYSREQ-PP-001
    type: system-req
    title: Byte stream pipe with read/write/reset/close
    status: approved
    description: >
      A byte stream pipe that matches the Zephyr k_pipe API semantics:
      initialization, write_check (validate and commit bytes),
      read_check (validate and consume bytes), reset, and close.
      Must maintain used/size capacity bounds and never exhibit undefined
      behavior or arithmetic overflow.
    tags: [pipe, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 10 properties (PP1-PP10) verified by Verus SMT proofs
        and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── Pipe: Software requirements (10 ASIL-D properties) ──────────────────

  - id: SWREQ-PP-PP01
    type: sw-req
    title: "PP1: 0 <= used <= size (capacity invariant)"
    status: approved
    description: >
      The used byte count is always between 0 and size inclusive.
      This is the fundamental invariant maintained across all operations.
      Maps to pipe.c:67-296.
    tags: [pipe, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures/invariant on all methods. Proptest random op sequences.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP02
    type: sw-req
    title: "PP2: size > 0 (always after init)"
    status: approved
    description: >
      The pipe buffer size is always strictly positive.
      Enforced by init rejecting size == 0 with -EINVAL.
      Maps to pipe.c:67-85.
    tags: [pipe, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus requires on init(). Unit test init_rejects_zero.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP03
    type: sw-req
    title: "PP3: write_check on closed pipe returns EPIPE"
    status: approved
    description: >
      When write_check is called on a pipe that is not open,
      returns -EPIPE without modifying state.
      Maps to pipe.c:147-218.
    tags: [pipe, write, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on write_check(). Unit test write_closed_returns_epipe.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP04
    type: sw-req
    title: "PP4: write/read on resetting pipe returns ECANCELED"
    status: approved
    description: >
      When write_check or read_check is called on a pipe with the
      reset flag set, returns -ECANCELED without modifying state.
      Maps to pipe.c:147-271.
    tags: [pipe, reset, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on write_check()/read_check(). Unit test resetting_returns_ecanceled.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP05
    type: sw-req
    title: "PP5: write_check computes correct byte count (min of request and free)"
    status: approved
    description: >
      On success, write_check returns n > 0 where n <= request_len
      and n <= free space.  used is incremented by n.
      Maps to pipe.c:147-218.
    tags: [pipe, write, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on write_check(). Unit test write_clamped_to_free_space.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP06
    type: sw-req
    title: "PP6: read_check computes correct byte count (min of request and used)"
    status: approved
    description: >
      On success, read_check returns n > 0 where n <= request_len
      and n <= used bytes.  used is decremented by n.
      Maps to pipe.c:220-271.
    tags: [pipe, read, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on read_check(). Unit test read_clamped_to_available.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP07
    type: sw-req
    title: "PP7: reset sets used to 0"
    status: approved
    description: >
      Reset clears the used count to zero and sets the reset flag.
      Maps to pipe.c:273-285.
    tags: [pipe, reset, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on reset(). Unit test reset_empties_pipe.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP08
    type: sw-req
    title: "PP8: close clears all flags"
    status: approved
    description: >
      Close sets flags to 0, meaning the pipe is neither open nor
      resetting.
      Maps to pipe.c:287-296.
    tags: [pipe, close, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on close(). Unit test close_clears_flags.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP09
    type: sw-req
    title: "PP9: conservation: used + free == size"
    status: approved
    description: >
      The sum of used bytes and free space always equals the buffer size.
    tags: [pipe, conservation, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures on space_get()/data_get(). Proptest conservation check.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  - id: SWREQ-PP-PP10
    type: sw-req
    title: "PP10: no arithmetic overflow in any operation"
    status: approved
    description: >
      No arithmetic operation on used or size causes integer overflow.
      All arithmetic is guarded by min() operations with capacity checks.
    tags: [pipe, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus overflow-free proof. Clippy arithmetic_side_effects lint.
    links:
      - type: derives-from
        target: SYSREQ-PP-001

  # ── Timer: System requirement ──────────────────────────────────────────

  - id: SYSREQ-TM-001
    type: system-req
    title: Timer status counter with init/start/stop/expire/status_get
    status: approved
    description: >
      A timer status counter that matches the Zephyr k_timer API semantics:
      initialization, start (reset status), stop (reset status and clear
      running), expire (increment status), and status_get (atomic read and
      reset).  Must maintain non-negative status and never exhibit undefined
      behavior or arithmetic overflow.
    tags: [timer, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 8 properties (TM1-TM8) verified by Verus SMT proofs
        and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── Timer: Software requirements (8 ASIL-D properties) ────────────────

  - id: SWREQ-TM-TM01
    type: sw-req
    title: "TM1: status >= 0 (non-negative)"
    status: approved
    description: >
      The timer status counter is always non-negative. This is the
      fundamental invariant maintained across all operations.
    tags: [timer, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures/invariant on all methods. Proptest random op sequences.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  - id: SWREQ-TM-TM02
    type: sw-req
    title: "TM2: status_get atomically reads and resets"
    status: approved
    description: >
      When status_get is called, the current status value is returned
      and the status counter is reset to zero in a single logical step.
    tags: [timer, status-get, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on status_get(). Unit test status_get_reads_and_resets.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  - id: SWREQ-TM-TM03
    type: sw-req
    title: "TM3: start resets status"
    status: approved
    description: >
      When start is called, the status counter is reset to zero
      regardless of its previous value.
    tags: [timer, start, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on start(). Unit test start_resets_status.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  - id: SWREQ-TM-TM04
    type: sw-req
    title: "TM4: stop resets status and clears running"
    status: approved
    description: >
      When stop is called, the status counter is reset to zero and the
      running flag is cleared.
    tags: [timer, stop, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on stop(). Unit test stop_clears_running_and_status.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  - id: SWREQ-TM-TM05
    type: sw-req
    title: "TM5: expire increments status by 1"
    status: approved
    description: >
      When expire is called, the status counter is incremented by exactly 1.
    tags: [timer, expire, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on expire(). Unit test expire_increments_status.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  - id: SWREQ-TM-TM06
    type: sw-req
    title: "TM6: period==0 means one-shot"
    status: approved
    description: >
      When the timer period is zero, the timer operates in one-shot mode
      and does not auto-restart after expiration.
    tags: [timer, one-shot, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on init/start(). Unit test period_zero_is_one_shot.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  - id: SWREQ-TM-TM07
    type: sw-req
    title: "TM7: period>0 means periodic"
    status: approved
    description: >
      When the timer period is greater than zero, the timer operates in
      periodic mode and auto-restarts after each expiration.
    tags: [timer, periodic, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on init/start(). Unit test period_nonzero_is_periodic.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  - id: SWREQ-TM-TM08
    type: sw-req
    title: "TM8: no overflow on expire"
    status: approved
    description: >
      No arithmetic operation on the status counter causes integer overflow.
      Expire uses checked or saturating arithmetic.
    tags: [timer, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus overflow-free proof. Clippy arithmetic_side_effects lint.
    links:
      - type: derives-from
        target: SYSREQ-TM-001

  # ── Event: System requirement ──────────────────────────────────────────

  - id: SYSREQ-EV-001
    type: system-req
    title: Event bitmask with post/set/clear/wait_any/wait_all
    status: approved
    description: >
      An event object that matches the Zephyr k_event API semantics:
      post (OR bits), set (replace), clear (AND complement), set_masked
      (selective update), wait_any (any bit match), and wait_all (all bits
      match).  Must maintain valid bitmask and never exhibit undefined
      behavior or arithmetic overflow.
    tags: [event, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 8 properties (EV1-EV8) verified by Verus SMT proofs
        and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── Event: Software requirements (8 ASIL-D properties) ────────────────

  - id: SWREQ-EV-EV01
    type: sw-req
    title: "EV1: post ORs bits"
    status: approved
    description: >
      When post is called with a bitmask, the event's current value is
      updated to the bitwise OR of the current value and the posted bits.
    tags: [event, post, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on post(). Unit test post_ors_bits.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  - id: SWREQ-EV-EV02
    type: sw-req
    title: "EV2: set replaces"
    status: approved
    description: >
      When set is called with a bitmask, the event's current value is
      replaced entirely with the given bitmask.
    tags: [event, set, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on set(). Unit test set_replaces_value.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  - id: SWREQ-EV-EV03
    type: sw-req
    title: "EV3: clear ANDs complement"
    status: approved
    description: >
      When clear is called with a bitmask, the event's current value is
      updated to the bitwise AND of the current value and the complement
      of the clear mask.
    tags: [event, clear, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on clear(). Unit test clear_ands_complement.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  - id: SWREQ-EV-EV04
    type: sw-req
    title: "EV4: set_masked selective"
    status: approved
    description: >
      When set_masked is called with a value and a mask, only the bits
      selected by the mask are updated to the corresponding bits from
      the value; unmasked bits are preserved.
    tags: [event, set-masked, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on set_masked(). Unit test set_masked_selective.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  - id: SWREQ-EV-EV05
    type: sw-req
    title: "EV5: wait_any condition"
    status: approved
    description: >
      wait_any is satisfied when the bitwise AND of the event value and
      the requested mask is non-zero (at least one requested bit is set).
    tags: [event, wait-any, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on wait_any(). Unit test wait_any_condition.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  - id: SWREQ-EV-EV06
    type: sw-req
    title: "EV6: wait_all condition"
    status: approved
    description: >
      wait_all is satisfied when the bitwise AND of the event value and
      the requested mask equals the requested mask (all requested bits
      are set).
    tags: [event, wait-all, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on wait_all(). Unit test wait_all_condition.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  - id: SWREQ-EV-EV07
    type: sw-req
    title: "EV7: valid bitmask"
    status: approved
    description: >
      The event value is always a valid u32 bitmask. All bitwise operations
      preserve the u32 domain with no undefined behavior.
    tags: [event, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus invariant on all methods. Proptest random op sequences.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  - id: SWREQ-EV-EV08
    type: sw-req
    title: "EV8: post monotonic"
    status: approved
    description: >
      Post can only set bits, never clear them. The resulting value after
      post always has a superset of the bits that were set before.
    tags: [event, post, monotonic, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on post(). Proptest monotonicity check.
    links:
      - type: derives-from
        target: SYSREQ-EV-001

  # ── MemSlab: System requirement ────────────────────────────────────────

  - id: SYSREQ-MS-001
    type: system-req
    title: Memory slab block counter with alloc/free
    status: approved
    description: >
      A memory slab allocator that matches the Zephyr k_mem_slab API
      semantics: initialization with num_blocks and block_size, alloc
      (increment used), and free (decrement used).  Must maintain
      block count bounds and never exhibit undefined behavior or
      arithmetic overflow.
    tags: [mem-slab, kernel]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        All 8 properties (MS1-MS8) verified by Verus SMT proofs
        and runtime test suites.
    links:
      - type: derives-from
        target: STKH-001

  # ── MemSlab: Software requirements (8 ASIL-D properties) ──────────────

  - id: SWREQ-MS-MS01
    type: sw-req
    title: "MS1: 0 <= num_used <= num_blocks (bounds invariant)"
    status: approved
    description: >
      The used block count is always between 0 and num_blocks inclusive.
      This is the fundamental invariant maintained across all operations.
    tags: [mem-slab, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures/invariant on all methods. Proptest random op sequences.
    links:
      - type: derives-from
        target: SYSREQ-MS-001

  - id: SWREQ-MS-MS02
    type: sw-req
    title: "MS2: num_blocks > 0 (always after init)"
    status: approved
    description: >
      The total number of blocks is always strictly positive.
      Enforced by init rejecting num_blocks == 0 with -EINVAL.
    tags: [mem-slab, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus requires on init(). Unit test init_rejects_zero_blocks.
    links:
      - type: derives-from
        target: SYSREQ-MS-001

  - id: SWREQ-MS-MS03
    type: sw-req
    title: "MS3: block_size > 0 (always after init)"
    status: approved
    description: >
      The block size is always strictly positive.
      Enforced by init rejecting block_size == 0 with -EINVAL.
    tags: [mem-slab, invariant, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus requires on init(). Unit test init_rejects_zero_block_size.
    links:
      - type: derives-from
        target: SYSREQ-MS-001

  - id: SWREQ-MS-MS04
    type: sw-req
    title: "MS4: alloc increments num_used by 1"
    status: approved
    description: >
      When alloc is called and num_used < num_blocks, num_used increments
      by 1 and returns OK.
    tags: [mem-slab, alloc, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on alloc(). Unit test alloc_increments_used.
    links:
      - type: derives-from
        target: SYSREQ-MS-001

  - id: SWREQ-MS-MS05
    type: sw-req
    title: "MS5: alloc full returns ENOMEM"
    status: approved
    description: >
      When alloc is called and num_used >= num_blocks, returns ENOMEM
      without modifying the slab state.
    tags: [mem-slab, alloc, error, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on alloc(). Unit test alloc_full_returns_enomem.
    links:
      - type: derives-from
        target: SYSREQ-MS-001

  - id: SWREQ-MS-MS06
    type: sw-req
    title: "MS6: free decrements num_used by 1"
    status: approved
    description: >
      When free is called and num_used > 0, num_used decrements by 1
      and returns OK.
    tags: [mem-slab, free, asil-d]
    fields:
      req-type: functional
      priority: must
      verification-criteria: >
        Verus ensures on free(). Unit test free_decrements_used.
    links:
      - type: derives-from
        target: SYSREQ-MS-001

  - id: SWREQ-MS-MS07
    type: sw-req
    title: "MS7: conservation (num_used + num_free == num_blocks)"
    status: approved
    description: >
      The sum of used and free blocks always equals the total number
      of blocks.
    tags: [mem-slab, conservation, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus ensures on num_free()/num_used(). Proptest conservation check.
    links:
      - type: derives-from
        target: SYSREQ-MS-001

  - id: SWREQ-MS-MS08
    type: sw-req
    title: "MS8: no overflow on alloc/free"
    status: approved
    description: >
      No arithmetic operation on num_used or num_blocks causes integer
      overflow. All arithmetic is guarded by bounds checks.
    tags: [mem-slab, safety, overflow, asil-d]
    fields:
      req-type: safety
      priority: must
      verification-criteria: >
        Verus overflow-free proof. Clippy arithmetic_side_effects lint.
    links:
      - type: derives-from
        target: SYSREQ-MS-001
