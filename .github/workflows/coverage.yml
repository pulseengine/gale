# Code coverage for Gale — Rust (cargo-llvm-cov) + Zephyr C (twister + gcov)
#
# Generates LCOV reports and uploads to Codecov.
# Rust coverage covers the verified kernel models + FFI crate.
# Zephyr coverage covers the C shims running on QEMU targets.

name: Coverage

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # ── Rust source coverage (cargo-llvm-cov) ─────────────────────────
  rust-coverage:
    name: Rust Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@nightly
      - uses: Swatinem/rust-cache@v2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov

      - name: Generate coverage (LCOV + HTML)
        run: |
          cargo llvm-cov --workspace --lcov --output-path lcov-rust.info
          cargo llvm-cov --workspace --html --output-dir coverage-rust

      - name: Upload LCOV to Codecov
        if: env.CODECOV_TOKEN != ''
        uses: codecov/codecov-action@v7
        with:
          files: lcov-rust.info
          flags: rust
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload HTML coverage report
        uses: actions/upload-artifact@v7
        with:
          name: coverage-rust
          path: coverage-rust/
          retention-days: 14

  # ── Zephyr C coverage (twister + gcov on QEMU) ───────────────────
  zephyr-coverage:
    name: Zephyr C Coverage
    runs-on: ubuntu-22.04
    container:
      image: ghcr.io/zephyrproject-rtos/ci-base:v0.29.0
      options: --user root --volume /mnt:/mnt
    env:
      HOME: /root
      DOCKER_CONFIG: /tmp/.docker
      GITHUB_TOKEN: ${{ github.token }}
      # ENOSPC durable fix (gale#73): rustup toolchains + the SDK download temp on
      # the big /mnt disk (workspace + SDK extract already moved to /mnt below).
      RUSTUP_HOME: /mnt/.rustup
      TMPDIR: /mnt/tmp
    steps:
      - name: Checkout Gale
        uses: actions/checkout@v6
        with:
          path: gale

      - name: Install Rust + ARM target
        run: |
          apt-get update -qq && apt-get install -y -qq curl > /dev/null 2>&1
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
          . /root/.cargo/env
          rustup target add thumbv7m-none-eabi

      - name: Initialize west workspace
        run: |
          pip3 install west gcovr
          mkdir -p /mnt/tmp   # TMPDIR for SDK download temp (gale#73)
          west init -m https://github.com/pulseengine/zephyr.git --mr gale/sem-replacement /mnt/zephyr-workspace
          cd /mnt/zephyr-workspace
          west update --narrow -o=--depth=1
          for attempt in 1 2 3; do west sdk install --install-base /mnt -t arm-zephyr-eabi --personal-access-token "${GITHUB_TOKEN}" && break || sleep 5; done

      - name: Run twister with coverage
        run: |
          . /root/.cargo/env
          export ZEPHYR_BASE="/mnt/zephyr-workspace/zephyr"
          export GALE_ROOT="${GITHUB_WORKSPACE}/gale"
          cd /mnt/zephyr-workspace
          # Run kernel tests on qemu_cortex_m3 with coverage enabled
          "${ZEPHYR_BASE}/scripts/twister" \
            --platform qemu_cortex_m3 \
            --tag kernel \
            --enable-coverage \
            --coverage-tool gcovr \
            --coverage-formats lcov \
            --outdir "${GITHUB_WORKSPACE}/twister-out" \
            --enable-slow \
            -v \
            || true

      - name: Upload LCOV to Codecov
        if: env.CODECOV_TOKEN != ''
        uses: codecov/codecov-action@v7
        with:
          directory: twister-out/coverage/
          flags: zephyr
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload twister results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: twister-results
          path: |
            twister-out/twister.json
            twister-out/coverage/
          retention-days: 14
